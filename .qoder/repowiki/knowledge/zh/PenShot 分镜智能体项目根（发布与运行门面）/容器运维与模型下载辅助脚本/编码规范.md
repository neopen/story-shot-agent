- 脚本通过解析自身所在目录向上推导仓库根路径（`Path(__file__).resolve().parents[1]` 或 `os.path.dirname(os.path.dirname(os.path.abspath(__file__)))`），再拼接 `requirements.txt`、`main.py`、`data/models/...` 等相对路径。
- 对外部依赖采用运行时 import（如 `from huggingface_hub import snapshot_download`、`from sentence_transformers import CrossEncoder`），仅在对应分支中加载，避免非必需依赖导致脚本无法导入。
- 所有输出统一经 `print(..., flush=True)` 或带 `[entrypoint]` 前缀的日志函数打印，确保容器日志实时可见。