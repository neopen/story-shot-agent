每个脚本均可单独执行，如 `python examples/direct_usage.py`、`python examples/web_app.py --port 8000 --reload`；MCP 测试需先启动 server 再运行 client（`mcp_client.py` 内部会 `subprocess.Popen([sys.executable, "-m", "penshot.mcp_server"])` 拉起子进程）。