version: '3.8'

services:
  story-shot-agent:
    build: .
    image: story-shot-agent:latest
    restart: unless-stopped
    ports:
      - "8000:8000"
    environment:
      API__HOST: "0.0.0.0"
      API__PORT: "8000"
    # If you want logs or persistent data, mount volumes below
    # volumes:
    #   - ./logs:/app/logs

